apiVersion: apps/v1
kind: Deployment
metadata:
  name: plannify-dev-postgresql
  namespace: plannify-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plannify-dev-postgresql
  template:
    metadata:
      labels:
        app: plannify-dev-postgresql
    spec:
      containers:
        - image: postgres:17
          name: plannify-dev-postgresql
          ports:
            - containerPort: 5432
          resources:
            requests:
              cpu: "20m"
              memory: "32Mi"
            limits:
              cpu: "60m"
              memory: "128Mi"
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: plannify-dev-postgresql
                  key: POSTGRES_DB
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: plannify-dev-postgresql
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: plannify-dev-postgresql
                  key: POSTGRES_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          volumeMounts:
            - name: postgres-pv
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-pv
          persistentVolumeClaim:
            claimName: plannify-dev-postgresql-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: plannify-dev-postgresql
  namespace: plannify-dev
spec:
  type: ClusterIP
  selector:
    app: plannify-dev-postgresql
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plannify-dev-postgresql-pvc
  namespace: plannify-dev
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
