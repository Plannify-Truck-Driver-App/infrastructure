apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-plannify-dev
spec:
  acme:
    email: contact@plannify.be
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: letsencrypt-plannify-dev
    solvers:
    - http01:
        ingress:
          class: traefik